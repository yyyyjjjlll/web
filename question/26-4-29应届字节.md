# 1 自我介绍
# 2 项目穿插八股
## 项目
1. 你平时的优化思路
2. 说一下对LCP的理解
3. （登陆了我的项目）上传图片的时候报错了，把错误发给我（payload too large）问我你觉得是什么问题。你要怎么验证这个问题。
4. 你怎么用ai coding
5. 平时用哪些工具
6. 有没有装skill
7. MCP和Skill是什么
8. 说了一下项目的内容，是干嘛的，用了一下。说好像又点卡啊。我说可能是服务器距离问题，他问怎么解决。
9. 项目用ai写的吗，你写的部分有多少，你觉得这个项目算你写的还是ai写的。
10. 怎么优化的prompt
11. 美团实习和你自己做项目有什么区别
12. 说模型结构化输出是什么意思
13. 用的什么模型api
## 八股
1. 浏览器上输入url到渲染的过程
2. （说到合成层了）合成层是什么
3. 哪些是触发合成层的。
4. Https怎么保证安全
5. tls过程会不会被攻击
6. get和post的区别
7. 304是什么
8. 讲协商缓存
9. 进程和线程的区别
10. 浏览器是什么，为什么是这个？
11. 数据库的事务
## 又回到项目了
1. 讲一下nextjs
2. 为什么你要拆开首页去ssr
3. 如果先渲染了边框，但是主要内容还是要等，这有用吗？
4. 能不能去测一个特定元素的渲染时间。
# 3 代码
1. 自己写树结构，判断A树里有没有B子树
2. 调度器
```
// 请求调度器 / Scheduler
// 实现一个调度器 Scheduler，最多同时 2 个任务执行，其他排队。

class Scheduler {
    
    add(promiseCreator) {}
}

// 能跑通这个例子
const scheduler = new Scheduler(2);
const timeout = (time) => new Promise(resolve => setTimeout(resolve, time));
const addTask = (time, order) => {
    scheduler.add(() => timeout(time)).then(() => console.log(order));
};

addTask(1000, '1');
addTask(500, '2');
addTask(300, '3');
addTask(400, '4');
```
