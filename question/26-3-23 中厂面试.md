# 面试题



### 1 请你用 3 分钟介绍一下你这份简历里最能代表你技术深度的一个项目。要求你讲清楚：

- 业务背景是什么
- 你负责的边界是什么
- 最难的技术问题是什么



### 2. SSE 流式会话与状态机设计

你在项目里提到：在多轮 Function Calling 场景下，为了解决 UI 与流式消息状态不同步的问题，你设计了有限状态机来管理消息生命周期。请详细讲一下：

- 这个“不同步”具体是怎样发生的？
- 你的状态机有哪些状态、事件、转移条件？
- 如果 tool call、文本流、错误中断、用户取消同时发生，状态机会怎么处理？

**追问：**

- 如果后端重复推送某个 chunk，或者 tool result 晚到，前端如何做到幂等？

---

### 3. Buffer 队列 + requestAnimationFrame 批量刷新
你写到通过 buffer 队列 + rAF 批量刷新，将渲染频次从峰值 120 次/秒降到 40 次/秒以下。请你讲一下：

- 原始问题是什么？为什么流式 chunk 会导致频繁渲染？
- 你的 buffer 结构怎么设计？
- 为什么选 requestAnimationFrame，而不是 setTimeout / debounce？
- 如果业务方要求“逐字输出”的实时感，这套方案会不会影响用户体验？

---

### 4. Markdown 补全与布局稳定
你提到做了：

- 预留骨架高度
- `overflow-anchor`
- 未闭合 Markdown 块自动补全

来解决流式输出时的布局抖动。请你讲讲：

- 抖动的根因是什么？
- 哪类 Markdown 内容最容易引起高度突变？
- “未闭合 Markdown 自动补全”是怎么做的？
- 为什么这个问题在 AI 聊天场景里比普通富文本更突出？

---

### 5. 长列表性能优化与动态高度虚拟列表
你提到针对 50+ 轮长对话，做了动态高度虚拟列表，结合 `ResizeObserver` 和离屏预计算，解决白屏和闪烁问题。请详细说明：

- 为什么普通虚拟列表在聊天场景里不够？
- 动态高度为什么难？
- 你如何维护 item 高度缓存？
- 快速滚动时白屏的根因是什么？你是怎么解决的？
- 为什么最终要配合游标分页？

**追问：**

- 你这里为什么没有直接使用现成库，而要做自定义增强？现成库的边界在哪里？

---

### 6. 跨框架 AI 组件库设计
你提到设计了一个跨 React/Vue 的 AI 聊天组件库，采用 Core 层 + 适配层 + 插槽体系。请讲一下：

- 为什么要做跨框架，而不是各写一套？
- Core 层和 Adapter 层的职责边界是什么？
- Block 化消息模型怎么定义？为什么适合 AI 对话？
- 你如何保证 React/Vue 侧 API 一致，而不牺牲各自框架体验？

**追问：**

- 如果未来要支持小程序 / RN / Web Components，你这个架构哪里最可能需要重构？

---

### 7. A2UI 与 Widget Registry
你提到主导了 A2UI 协议前端架构设计，并通过白名单 Widget Registry 做到零成本接入。请解释：

- A2UI 的本质是什么？
- 为什么协议驱动渲染会比传统页面开发更适合 Agent 场景？
- Widget Registry 为什么要做白名单？
- 如果后端给了一个未知组件类型，前端如何处理？

**追问：**

- 如果某个 Widget 有副作用，比如调用支付、提交审批，A2UI 体系里前端应该怎么防风险？

---

### 8. MCP Gateway 与工具接入平台
你写到做了一个基于 JSON Schema 的可视化工具配置平台，把 function call 接入周期缩短到 10 分钟。请你讲：

- 这个平台解决的是前端问题、后端问题，还是平台化问题？
- JSON Schema 在这里承担什么角色？
- 一个 API 从“存量接口”变成“可被 Agent 调用的工具”，中间要经历哪些步骤？
- 你如何保证参数合法性、权限与调用安全？

**追问：**

- 如果后端接口定义变了，但 Schema 没同步，前端/Agent 会出现什么问题？你如何防止这种漂移？



### 9. React 宿主化混合渲染

你在客服项目里做了从 “Amis 全控应用” 到 “React Host + Amis Embed” 的迁移。请讲讲：

- 为什么不继续让 Amis 做主宿主？
- React Host 的价值是什么？
- 迁移过程中最难的是路由、状态还是生命周期？
- 你怎么判断“哪些部分保留低代码，哪些部分必须 React 化”？

---

### 10. React Router 接管低代码内置路由
你提到通过拦截 `jumpTo/updateLocation` 并代理到 `navigate()`，实现路由控制权平滑迁移。这里问你两个问题：

- 为什么前端路由控制权这么重要？
- 如果旧系统里有很多依赖路由参数的页面，你怎么确保迁移不出问题？

**追问：**

- 如果页面刷新后丢失某些运行时上下文，你怎么恢复？

---

### 11. E2E 自动化与 AI 代码防退化
你提到引入了：

- code-simplifier-agent 做语义清洗
- Playwright 做 E2E
- CDP + SourceMap 做覆盖率反解

请你讲讲：

- 为什么 AI 生成代码特别需要这套闭环？
- 语义级代码清洗和 ESLint/Prettier 有什么区别？
- E2E 覆盖率 90% 以上，为什么还可能漏掉问题？
- 你们如何选择关键路径场景？

---

### 12. React Hooks 原理与工程使用
结合你的项目经验，说一下：

- 为什么 Hook 不能写在条件分支里？
- `useRef` 和 `useState` 在流式会话场景里的职责有什么区别？
- 如果一个消息流组件出现闭包陷阱，你通常会怎么排查？



### 13. RAG 过程可视化
你提到前端会把 `Thought / Action / Observation` 映射成步骤条和可折叠推理卡片。这里问你：

- 为什么要把 RAG / Agent 过程可视化？
- 可视化会不会泄露模型内部推理，带来风险？
- 对客服场景来说，展示哪些信息最有价值？哪些反而不应该展示？

---

### 14. Agent 双区协作工作台
你提到设计了“智能助手区 + 人工工作区”的双区协作。请你回答：

- 为什么不直接让 Agent 全自动处理？
- 所谓“高置信度场景”如何定义？
- 当前端接到 Agent 自动降级信号时，UI 应该如何设计，才能让客服接手足够顺滑？

**追问：**

- 如果 Agent 推荐和人工最终决策不一致，你会如何记录并反哺系统？

---

### 15. 如果让你重新设计一个 AI 前端中台
假设现在让你从 0 到 1 再做一次 AI 前端中台，你会优先抽哪三层？为什么？



### 16. 指标真实性追问
你简历里有不少量化结果，比如：

- 接入成本从 2 人日降到 0.5 人日
- 渲染频次从 120 次/秒降到 40 次/秒以下
- E2E 场景覆盖率 90%+

请你任选一个指标，说明：

- 怎么定义的
- 怎么测量的
- 实验基线是什么
- 有没有可能这个数字被高估

---

### 17. 架构取舍追问
如果我让你现在现场简化你这个项目方案，你会砍掉哪两个“看起来高级，但不是第一阶段必须做”的设计？
